# onepoint

A test repository for interviews in python.

## First time setup

Install with development dependencies:

```
pip install -e ".[dev]"
```

## Run the tests

```
pytest
```

## Run the app

```
py -m onepoint
```
